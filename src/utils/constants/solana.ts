/** Public Solana mainnet RPC used as default and as a read fallback when the primary RPC rejects requests (e.g. 403 from browser). */
export const DEFAULT_MAINNET_RPC = 'https://api.mainnet-beta.solana.com';
